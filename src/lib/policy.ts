export type Policy={name:string;maxSol:number;maxSlippageBps:number;dailyLimitSol:number;allowedPrograms:string[];blockedDestinations:string[]};
export type Intent={amountSol:number;slippageBps:number;program:string;destination:string};
export function evaluateIntent(policy:Policy,intent:Intent){const checks=[{rule:"Program allowlist",pass:policy.allowedPrograms.includes(intent.program),detail:intent.program},{rule:"Per-transaction ceiling",pass:intent.amountSol<=policy.maxSol,detail:`${intent.amountSol} / ${policy.maxSol} SOL`},{rule:"Slippage ceiling",pass:intent.slippageBps<=policy.maxSlippageBps,detail:`${intent.slippageBps} / ${policy.maxSlippageBps} bps`},{rule:"Destination blocklist",pass:!policy.blockedDestinations.includes(intent.destination),detail:intent.destination||"No override"}];return{approved:checks.every(check=>check.pass),checks}}
export async function hashPolicy(policy:Policy){const bytes=new TextEncoder().encode(JSON.stringify(policy));const digest=await crypto.subtle.digest("SHA-256",bytes);return Array.from(new Uint8Array(digest)).map(byte=>byte.toString(16).padStart(2,"0")).join("")}
