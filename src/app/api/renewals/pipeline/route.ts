import{NextResponse}from"next/server";import{renewalSnapshot,runRenewalSweep}from"@/lib/mvp-store";export async function GET(){return NextResponse.json({policies:renewalSnapshot(),generated_at:new Date().toISOString()})}export async function POST(request:Request){if(process.env.CRON_SECRET&&request.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json({...runRenewalSweep(),idempotent:true,completed_at:new Date().toISOString()})}
