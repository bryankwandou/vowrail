import{NextResponse}from"next/server";import{z}from"zod";import{fulfillRequirement}from"@/lib/mvp-store";const schema=z.object({requirement_id:z.string().min(1),evidence:z.string().min(8)});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Requirement and meaningful evidence are required."},{status:400});try{return NextResponse.json(fulfillRequirement(id,parsed.data.requirement_id,parsed.data.evidence))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to fulfill requirement."},{status:404})}}
