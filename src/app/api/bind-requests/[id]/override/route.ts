import{NextResponse}from"next/server";import{z}from"zod";import{overrideAndBind}from"@/lib/mvp-store";const schema=z.object({justification_note:z.string(),role:z.enum(["agency_principal","licensed_agent","account_manager"]),delegated:z.boolean().default(false)});export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Role and justification are required."},{status:400});try{const result=overrideAndBind(id,parsed.data.role,parsed.data.delegated,parsed.data.justification_note);return NextResponse.json(result,{status:result.ok?200:403})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to apply override."},{status:404})}}
