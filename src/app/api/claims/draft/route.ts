import Groq from"groq-sdk";import{NextResponse}from"next/server";import{z}from"zod";import{createClaim}from"@/lib/mvp-store";import{groundedClaimsDraft}from"@/lib/domain";const schema=z.object({policy_id:z.string().min(1),raw_client_statement:z.string().min(10)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Policy ID and a meaningful client statement are required."},{status:400});let draft:Record<string,unknown>=groundedClaimsDraft(parsed.data.raw_client_statement);let engine="deterministic-grounded";if(process.env.GROQ_API_KEY){try{const groq=new Groq({apiKey:process.env.GROQ_API_KEY});const completion=await groq.chat.completions.create({model:process.env.GROQ_MODEL||"llama-3.3-70b-versatile",temperature:0,response_format:{type:"json_object"},messages:[{role:"system",content:"Draft insurance claims intake using only facts in the source statement. Never infer fault, cause, extent, injury, parties, or timing. For absent facts write Not reported. Return JSON keys date_time_of_loss, description_of_loss, parties_involved, immediate_actions, unsupported_details, review_required."},{role:"user",content:parsed.data.raw_client_statement}]});const content=completion.choices[0]?.message?.content;if(content){draft=JSON.parse(content);engine="groq-grounded"}}catch{engine="deterministic-fallback"}}const claim=createClaim(parsed.data.policy_id,parsed.data.raw_client_statement,draft);return NextResponse.json({...claim,drafted_intake:claim.draft,engine})}
