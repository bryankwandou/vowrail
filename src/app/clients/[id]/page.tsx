import{AppShell}from"@/components/app-shell";export default async function ClientPage({params}:{params:Promise<{id:string}>}){const{id}=await params;return <AppShell active="Clients"><main className="workspace"><div className="workspace-head"><div><span className="kicker">Client · {id}</span><h1>Maya Chen</h1><p>Client since 2019 · Two active policies · Preferred contact: email</p></div><span className="status status-good">Account current</span></div><section className="panel empty-state"><h2>Client policy record</h2><p>Homeowners policy POL-8821 is nine days from expiration and currently awaiting a renewal response.</p></section></main></AppShell>}
